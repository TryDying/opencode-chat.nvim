# 更新日志

本文件用于记录面向用户的 release-style 项目变更。

当前项目尚未发布版本。

## Unreleased

- 实现基于 Neovim 原生 floating terminal 的 opencode TUI MVP。
- 支持 `:OpencodeToggle`、`:OpencodeAppendFile`、`:OpencodeAppendSelection`、`:OpencodeNewSession`、`:OpencodeStop`。
- 支持通过 `/tui/append-prompt` 追加当前文件或 Visual 选区引用。
- 增加不读取全局 Neovim 配置的自闭环测试。
