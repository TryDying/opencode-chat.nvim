# 开发指南

## 当前状态

仓库目前处于初始化阶段，尚未加入 Lua 插件代码、测试框架、lint/format 配置或包管理脚本。

## 本地开发准备

需要的基础环境：

- Neovim，版本以后续实现实际使用的 API 为准。
- opencode CLI，需支持 `opencode --port <port>` 和 `/tui/append-prompt`。
- `curl`，MVP 初期可用于验证本地 HTTP bridge。

## 计划中的使用方式

插件实现后，用户侧配置预计类似：

```lua
require("opencode_chat").setup()

vim.keymap.set("n", "<M-->", function()
  require("opencode_chat").toggle()
end, { desc = "Toggle opencode" })

vim.keymap.set("v", "<M-->", function()
  require("opencode_chat").append_selection()
end, { desc = "Append selection to opencode" })
```

## 开发顺序

建议按以下顺序推进 MVP：

1. 建立 Lua 模块骨架和 `setup()`。
2. 实现配置合并。
3. 实现 project root 检测。
4. 实现端口选择和 opencode job 生命周期。
5. 实现 Neovim 原生 floating terminal。
6. 实现 `/tui/append-prompt` HTTP client。
7. 实现当前文件和 Visual 选区引用生成。
8. 注册命令和补充 README 使用说明。

## 验证方式

当前没有自动化测试命令。MVP 的最小手动验证包括：

1. 在一个项目内打开 Neovim。
2. 执行 `:OpencodeToggle`，确认 opencode TUI 打开。
3. 重复 toggle，确认只是隐藏/显示同一个 TUI，不创建新 session。
4. Visual 选择几行后触发追加，确认输入框出现 `@relative/path#Lx-Ly`。
5. 确认追加后没有自动提交。
6. 反向 Visual 选择后确认输出行号仍为升序。

后续如果加入 `luacheck`、`stylua` 或测试框架，应在本文件补充准确命令。
